// project import
import defaults from './defaults';
import examples from './examples';
import settings from './settings';

const menuItems = {
  items: [defaults,examples, settings]
};

export default menuItems;
